node_modules
dist
out
release
.next
.env
.env.*
!.env.example
# Las plantillas de entorno SÍ van al repo (deploy/.env.app.example): son documentación,
# no secretos. Sin esta excepción, `.env.*` de arriba se las traga en silencio y el
# runbook de despliegue queda cojo.
!**/*.example
.env.test
.turbo
.superpowers/
test-results/
playwright-report/
blob-report/
playwright/.cache/
.claude/*
# ...salvo la config de preview (dev server), que sí es compartida y útil en cualquier máquina.
!.claude/launch.json

# Volcados de catálogo de clientes para importar: datos de negocio ajenos, no van al repo.
.import/
